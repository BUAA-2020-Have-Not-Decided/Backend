<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.buaa.scholarshipserver.dao.UserMapper">
  <resultMap id="BaseResultMap" type="cn.edu.buaa.scholarshipserver.models.User">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <id column="UserID" jdbcType="INTEGER" property="UserID" />
    <result column="Password" jdbcType="VARCHAR" property="Password" />
    <result column="Name" jdbcType="VARCHAR" property="Name" />
    <result column="Sex" jdbcType="INTEGER" property="Sex" />
    <result column="UserImagePath" jdbcType="VARCHAR" property="UserImagePath" />
    <result column="Email" jdbcType="VARCHAR" property="Email" />
    <result column="BriefIntroduction" jdbcType="VARCHAR" property="BriefIntroduction" />
    <result column="Identify" jdbcType="INTEGER" property="Identify" />
  </resultMap>

  <select id="getUserByID" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select * from User where UserID=#{v}
  </select>

  <select id="getUserByName" parameterType="java.lang.String" resultMap="BaseResultMap">
    select * from User where Name=#{v}
  </select>

  <insert id="insertNewUser" parameterType="cn.edu.buaa.scholarshipserver.models.User">
    insert into User values(Password=#{Password}, Name=#{Name}, Sex=#{Sex}, UserImagePath=#{UserImagePath}, Email=#{Email}, BriefIntroduction=#{BriefIntroduction}, Identity=#{Identity})
  </insert>

  <update id="updatePassword" parameterType="map">
    update User set Password = #{Password} where UserID=#{ID}
  </update>

  <update id="updateIntro" parameterType="map">
    update User set BriefIntroduction=#{Intro} where UserID=#{ID}
  </update>

  <update id="updateName" parameterType="map">
    update User set Name=#{Name} where UserID=#{ID}
  </update>

  <update id="updateImagePath" parameterType="map">
    update User set UserImagePath=#{Path} where UserID=#{ID}
  </update>
</mapper>